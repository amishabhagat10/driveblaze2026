export default function Contact() {
  return (
    <section style={sectionStyle}>
      <h1>Contact</h1>
      <p>Let’s connect</p>
    </section>
  );
}

const sectionStyle = {
  minHeight: "100vh",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};